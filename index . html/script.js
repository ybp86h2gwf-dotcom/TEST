function showMessage() {
    const button = document.querySelector('button');

    button.textContent = 'Let\'s learn together';
    button.setAttribute('aria-label', 'Learning interest confirmed');
}
